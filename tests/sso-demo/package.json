{
  "name": "sso-demo",
  "version": "1.0.0",
  "dependencies": {
    "@sap/cds": "^9",
    "@sap/xssec": "^4"
  },
  "devDependencies": {
    "@cap-js/sqlite": "^2",
    "@sap/cds-dk": "^9"
  },
  "scripts": {
    "start": "cds-serve",
    "build:cds": "cds build --production",
    "build:mta": "mbt build -t gen",
    "deploy:cf": "cf deploy gen/sso-demo_1.0.0.mtar --abort-on-error --delete-services"
  },
  "private": true,
  "cds": {
    "requires": {
      "[production]": {
        "auth": "xsuaa"
      }
    }
  }
}
